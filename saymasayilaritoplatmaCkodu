#include <stdio.h>

int sonuc(int k);

int main() {
    int toplam = sonuc(10);
    printf("sonuc = %d",toplam);
    return 0;


}

int sonuc(int k) {
if (k > 0) {
    return k + sonuc(k-1);
}else {
return 0;
}


}
